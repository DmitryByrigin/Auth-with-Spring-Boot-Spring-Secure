version: '3.8'
services:
  postgres:
    image: postgres
    restart: always
    environment:
      - 'POSTGRES_USER=admin'
      - 'POSTGRES_PASSWORD=admin'
      - 'POSTGRES_DB=learn_spring'
    ports:
      - '5432:5432'
